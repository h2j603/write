# CLAUDE.md

이 리퍼지토리는 한국어 문장 첨삭·교정 및 한↔영 번역 작업용이다.

## 작업 방식

- 표기·교정 기준은 [`STYLE.md`](./STYLE.md)를 따른다.
- 첨삭·번역 요청에는 **수정문 → 변경 사항 → (필요 시) 대안** 순으로 답한다.

## 중요 규칙

- **첨삭·번역 결과물은 채팅으로만 제공하고 커밋하지 않는다.**
  파일 저장·커밋은 사용자가 명시적으로 요청할 때만 한다.
  (기준 문서 `STYLE.md`·`README.md` 등 설정/규칙 변경은 예외로 커밋 가능.)
- **줄표(`—`)를 남발하지 않는다.** AI가 쓴 티가 나므로 되도록 쉼표·마침표·괄호로 풀거나
  문장을 나눈다. 첨삭 문장과 채팅 답변 모두에 적용한다.
