# 한국어 문장 첨삭·교정 기준 (STYLE.md)

이 문서는 이 리퍼지토리에서 문장을 첨삭·교정할 때 적용하는 표기 기준이다.
기본 골격은 **「국립현대미술관 출판 지침」**(MMCA Guide to Publication)을 따르고,
어문 규범은 **국립국어원**의 규정을 근거로 한다.

> ⚠️ 표시가 있는 항목은 원문 지침(2판/PDF)으로 최종 확인이 필요한 부분이다.
> 출처 PDF가 외부 접속 차단으로 직접 대조되지 않아, 통용되는 관례로 우선 작성했다.
> 실제 지침과 다른 부분이 있으면 이 문서를 수정해 기준을 갱신한다.

---

## 0. 적용 우선순위

문장을 다듬을 때 아래 순서로 기준을 적용한다.

1. **국립현대미술관 출판 지침** + **서울시립미술관(SeMA) 표기 표준** — 미술·전시·출판 분야 표기(작가명·작품명·전시명·캡션 등)의 **뼈대**
2. **국립국어원 「한글 맞춤법」 / 「표준어 규정」** — 맞춤법·띄어쓰기·표준어
3. **「외래어 표기법」 / 「국어의 로마자 표기법」** — 외래어·고유명사 로마자
4. **열린책들 편집 매뉴얼** — 띄어쓰기·외래어·문장부호의 **세부 용례 보조 사전**(위 기준과 충돌하지 않는 범위에서 참조)
5. 기준이 충돌하거나 모호하면 **미술관 출판 관례**를 우선하고, 그 판단 근거를 첨삭 메모에 남긴다.

> 이 기준은 국립현대미술관·서울시립미술관 지침과 열린책들 편집 매뉴얼을 비교해
> **하나의 방향으로 통합**한 것이다. 세 기준의 차이 비교는 문서 끝 [부록](#부록-세-기준-비교)에 둔다.

---

## 1. 일반 지침

- 한 문장에 하나의 메시지를 담는 것을 원칙으로 하고, 지나치게 긴 문장은 끊는다.
- 번역 투(피동·사동 남용, "~에 대하여", "~을 통하여", "~에 의해" 등)와 군더더기를
  줄여 간결하게 다듬되, 글쓴이의 어조·의도는 보존한다.
- 피동 표현의 중복(이중 피동: "되어지다", "잊혀지다" → "되다", "잊히다")을 바로잡는다.
- 한 글 안에서 같은 대상을 가리키는 용어·표기는 **일관되게** 통일한다.
- **AI가 쓴 티가 나는 문체를 피한다.** 줄표(`—`) 남발, 상투적 대구("~가 아니라 ~다"),
  과한 강조, 불필요한 요약 문장 등을 자제하고, 사람이 쓴 듯 자연스럽고 담백하게 다듬는다.

## 2. 띄어쓰기

- 기본은 「한글 맞춤법」 제5장(띄어쓰기)을 따른다.
- 단위 명사는 띄어 쓴다: `한 점`, `세 명`, `2019년`(숫자+단위 결합은 붙임 가능).
- 의존 명사는 띄어 쓴다: `할 수 있다`, `아는 것`, `떠난 지 오래`.
- 보조 용언은 띄어 씀을 원칙으로 하되, 붙여 씀도 허용한다: `도와 드리다 / 도와드리다`.
- 고유 명사·전문 용어는 단위별로 띄어 씀이 원칙이나 붙여 쓸 수 있다. ⚠️ 미술관 관례 확인
- 성과 이름은 붙여 쓰고, 호칭·관직명은 띄어 쓴다: `홍길동`, `홍길동 관장`.

## 3. 고유명사 표기

### 3.1 작가명
- 한국 작가: 한글 이름. 처음 나올 때 필요하면 `한글(원어/한자, 생몰년)` 병기. ⚠️
- 외국 작가: 「외래어 표기법」에 따른 한글 표기. 처음 나올 때 `한글(원어 표기, 생몰년)` 병기.
  예) `요제프 보이스(Joseph Beuys, 1921–1986)`
- 본문에서 두 번째 언급부터는 성(姓)만 쓰는 것을 기본으로 한다. ⚠️

### 3.2 작품명 · 전시명 · 출판물명 — 부호 사용
국립국어원/미술관 통용 기준:

| 대상 | 부호 | 예시 |
| --- | --- | --- |
| 개별 미술 작품, 영화, 노래, 단일 곡 | 홑화살괄호 `〈 〉` | `〈수련〉`, `〈게르니카〉` |
| 전시 제목, 총서, 음반 | 겹화살괄호 `《 》` | `《올해의 작가상 2024》` |
| 책·정기간행물(단행본, 잡지, 신문) | 겹낫표 `『 』` | `『국립현대미술관 출판 지침』` |
| 책 속 글·논문·단편·장 제목 | 홑낫표 `「 」` | `「편집 지침」` |

- 본문에 작품명이 처음 나올 때 `〈작품명〉(연도)` 형식으로 연도를 병기할 수 있다.
- **로마자(원어) 제목은 부호 대신 이탤릭으로 쓴다.** 출판 관례상 서양어 작품명·서명·정기간행물명은
  낫표/화살괄호를 쓰지 않고 이탤릭 처리한다. 마크다운에서는 `*...*`로 적는다.
  예) `*The Starry Night*`, `*October*`(저널), 한글 병기 시 `〈별이 빛나는 밤〉(*The Starry Night*, 1889)`.

### 3.3 기관명
- 국립현대미술관은 처음에 전체 명칭, 이후 `미술관` 또는 약칭으로 받을 수 있다. ⚠️
- 외국 기관은 한글 표기 후 처음에 원어를 병기한다.

## 4. 숫자와 날짜

**원칙(열린책들식 한글 수 우선):**
- **서술 문장의 적은 수는 한글로 쓴다.** 예) `작품 세 점`, `두세 명`, `한두 해`, `네 번째 방`.
- **연도·치수·통계·쪽수·금액 등 수치 정보는 아라비아 숫자로 쓴다.** 예) `1960년대`, `35.5 × 45.0 cm`, `관람객 1,254명`, `120쪽`.
- 큰 수는 만 단위로 끊어 읽되 표기는 아라비아 숫자에 세 자리마다 쉼표: `12,000`, `1만 2000`(혼합 표기는 글 단위로 통일).
- 경계가 애매하면 **가독성**을 기준으로 판단하고, 한 글 안에서 일관되게 통일한다.

- 연도: `2026년`. 기간·범위는 붙임표(엔대시) `–` 사용: `1921–1986`, `2024–2025`.
  (하이픈 `-`이 아니라 엔대시 `–`를 쓴다.)
- 날짜: `2026년 6월 28일`. 숫자만 쓸 때 `2026. 6. 28.`(마침표 뒤 한 칸, 끝에도 마침표).
- 백분율은 `50%`(붙임), 수와 단위 사이는 관례에 따른다: `35.5 × 45.0 cm`.
- 크기 표기는 **세로 × 가로**(필요 시 ×깊이) 순, 단위는 `cm`. 곱셈 기호는 `×`(엑스 아님).

## 5. 문장 부호와 기호

- 마침표 `.`, 쉼표 `,`, 가운뎃점 `·`(열거·결합), 쌍점 `:`, 줄임표 `…`(말줄임표, `...` 대신).
- 따옴표: 큰따옴표 `" "`(직접 인용·대화), 작은따옴표 `' '`(강조·재인용).
- 붙임표/줄표 구분: 하이픈 `-`, 엔대시 `–`(범위), 엠대시 `—`(문장 삽입).
- **줄표(`—`)를 남발하지 않는다.** 엠대시로 삽입구를 넣는 습관은 AI가 쓴 티가 나므로, 되도록
  쉼표·마침표·괄호로 문장을 끊거나 문장을 둘로 나눈다. 줄표는 꼭 필요할 때만 드물게 쓴다.
  (이 규칙은 첨삭 대상 문장뿐 아니라 채팅 답변에도 똑같이 적용한다.)
- 괄호 안 보충 설명은 `( )`, 원어·연도 병기에 사용.
- 단위 기호 앞 띄어쓰기는 관례에 따라 통일한다. ⚠️

## 5.5 외래어 표기

**원칙(표기법 기본 + 관용 일부 허용):**
- 기본은 「외래어 표기법」과 『표준국어대사전』 표기를 따른다.
  예) `콘셉트`(○) / `컨셉`(×), `카페`, `보이스`, `메시지`, `재즈`, `로봇`.
- 외래어에는 **된소리를 쓰지 않는다.** 예) `파리`(○) / `빠리`(×), `서비스`(○) / `써비스`(×).
- 다만 **이미 굳어진 관용 표기·원음 표기는 일부 허용**한다. 표기법과 관용이 충돌할 때는
  대상 독자와 분야 관례를 고려해 택하되, **한 원고 안에서는 반드시 한 표기로 통일**한다.
  (관용을 택한 경우 첨삭 메모에 근거를 남긴다.)
- 원어 병기: 인명·지명·전문 용어는 처음 나올 때 `한글(원어)`로 병기하고 이후 한글로 통일한다.

## 6. 영문 표기

- 영문 고유명사·기관명은 원어 철자를 그대로 쓰고, 대소문자 규칙(Title Case 등)을 지킨다.
- 한글 본문 속 영문은 처음 등장 시 병기하고, 이후 한글로 통일한다.
- 영문 약어는 처음에 `전체 명칭(약어)` 형태로 풀어 쓴다. 예) `국립현대미술관(MMCA)`.

## 7. 국어의 로마자 표기

- 「국어의 로마자 표기법」을 따른다.
- 인명은 성과 이름 사이를 띄우고 이름은 붙여 쓴다: `Hong Gildong` 또는 `Hong Gil-dong`. ⚠️ 미술관 채택형 확인
- 지명·기관명은 표기법의 행정 단위 규칙을 따른다.

## 8. 발췌와 인용

- 짧은 인용은 본문에 큰따옴표로, 긴 인용(대체로 3행 이상)은 별행 인용(들여쓰기)으로 처리한다. ⚠️ 행수 기준 확인
- 인용문 안의 생략은 `[…]`로 표시한다.
- 인용자가 보충한 말은 대괄호 `[ ]`로 표시한다.
- 원문 강조와 인용자 강조를 구분해 표기한다.

## 9. 주(註)

- 각주/미주 형식을 글 단위로 통일한다.
- 주 번호는 해당 어구 뒤, 문장 부호 다음에 붙인다. ⚠️ 위치 규칙 확인

## 10. 참고 문헌

- 저자, 『서명』, 옮긴이, 출판지: 출판사, 출판 연도, 쪽수 순으로 정리한다. ⚠️ 정확한 순서·구두점 확인
- 같은 글 안에서 동일한 서지 양식을 일관되게 유지한다.

## 11. 캡션(도판 설명)

국내 미술 캡션 기본 순서(국립현대미술관/국내 관례):

```
작가명, 〈작품명〉, 제작연도, 매체(재료), 크기(세로 × 가로 cm), 소장처
```

예)
```
홍길동, 〈무제〉, 2024, 캔버스에 유채, 162.0 × 130.0 cm, 국립현대미술관 소장
```

- 항목 구분은 쉼표 `,`, 끝에는 마침표를 두지 않는 것을 기본으로 한다. ⚠️ 확인
- 크기는 세로 × 가로 순, 단위 `cm`. 영상·설치 등은 매체 특성에 맞춰 러닝타임·가변 크기 등 표기.
- 소장처가 작가/미술관일 때 `소장`, 대여작일 때 `제공`/`courtesy` 등 구분. ⚠️

## 12. 마크다운 출력 형식

최종 산출물은 **마크다운**이다. 서식은 한글 부호가 아니라 마크다운 문법으로 표현한다.

- **이탤릭**(강조, 로마자 작품명·서명, 학명·외래어 강조 등): `*텍스트*` 또는 `_텍스트_`
- **굵게**(중요어 강조): `**텍스트**`
- **굵은 이탤릭**: `***텍스트***`
- 제목·소제목: `#`, `##`, `###` …
- 목록: `-`(순서 없음), `1.`(순서 있음) / 인용: `>` / 코드·기호: `` `텍스트` ``
- 표가 필요한 캡션·목록은 마크다운 표(`| … |`)로 정리한다.

**부호와 서식의 구분**
- 한글 작품·전시·도서명은 **부호**(`〈 〉`·`《 》`·`『 』`·`「 」`)를 그대로 쓴다(마크다운 이탤릭으로 대체하지 않는다).
- 로마자(원어) 작품·서명은 **이탤릭**(`*...*`)으로 쓴다(부호를 쓰지 않는다).
- 일반 강조는 이탤릭/굵게를, 줄바꿈·들여쓰기 인용은 마크다운 인용(`>`)을 쓴다.

---

## 첨삭 결과 표기 방식

채팅으로 첨삭을 요청하면 기본적으로 다음 형식으로 답한다.

1. **수정문** — 다듬은 최종 문장
2. **변경 사항** — 무엇을 왜 고쳤는지 (적용한 규칙/근거)
3. 필요 시 **대안** — 어조·길이 등 선택지 제시

**첨삭·번역 결과물은 기본적으로 채팅으로만 제공하고, 리퍼지토리에 커밋하지 않는다.**
파일로 저장·커밋하는 것은 사용자가 명시적으로 요청할 때만 한다.

---

## 부록: 세 기준 비교

이 통합 기준을 만들 때 비교한 자료와 차이, 그리고 채택한 방향이다.

| 항목 | 국립현대미술관 출판 지침 | 서울시립미술관(SeMA) | 열린책들 편집 매뉴얼 | **이 리퍼지토리의 채택** |
| --- | --- | --- | --- | --- |
| 성격 | 미술 출판 실무 지침 | 소장품 기술·표기 표준화 | 문학·단행본 교열 매뉴얼 | 미술관 지침을 뼈대로, 열린책들을 보조 사전으로 |
| 어문 규범 | 국립국어원 | 국립국어원 | 국립국어원 + 자체 표제어 | 국립국어원 기준 |
| 작품명 | `〈 〉` | `〈 〉` | `「 」`/`〈 〉` | **`〈 〉`** |
| 전시명 | `《 》` | `《 》` | (해당 없음) | **`《 》`** |
| 책·정기간행물 | `『 』` | `『 』` | `『 』` | **`『 』`** |
| 글·논문·단편 | `「 」` | `「 」` | `「 」` | **`「 」`** |
| 본문 숫자 | 아라비아 경향 | 아라비아(데이터) | **한글 수 선호** | **한글 수 우선**(수치는 아라비아) |
| 외래어 | 표기법 준수 | 표기법 준수 | 표기법 + **관용 일부** | **표기법 기본 + 관용 일부 허용** |
| 강점 | 미술 출판 전반·캡션 | 소장품 데이터 일관성 | 띄어쓰기·외래어·문장부호 용례 | — |

**통합 방향 요약**
- 부호 체계는 세 기준이 사실상 일치 → 미술관 표준(`〈 〉`/`《 》`/`『 』`/`「 」`) 그대로 채택.
- 본문 숫자는 **열린책들식 한글 수 우선**, 연도·치수·통계 등 수치는 아라비아로 통일.
- 외래어는 표기법을 기본으로 하되 굳어진 관용 표기를 일부 허용하고, 원고 내 일관성을 우선.

**참고 자료**
- 「국립현대미술관 출판 지침」(MMCA Guide to Publication)
- 「서울시립미술관 소장품 기술지침」(SeMA)
- 「열린책들 편집 매뉴얼」(열린책들 편집부)
- 국립국어원 「한글 맞춤법」·「표준어 규정」·「외래어 표기법」·「국어의 로마자 표기법」

> 일부 원문(유료 도서·접근 제한 PDF)은 직접 대조하지 못해 통용 관례로 정리한 항목이 있다(본문 ⚠️ 표시). 원문 확인 시 이 기준을 갱신한다.
